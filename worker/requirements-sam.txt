# Optional dependencies for the SAM2 click-to-select subject feature.
# Install in the worker image with:
#   docker build --build-arg INSTALL_SAM2=1 -t worker ./worker
#
# Adds ~1.5GB to the image. SAM2 on CPU is multiples slower than GPU; we
# log a warning and recommend a GPU host for clips longer than ~30s.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchvision==0.20.1
hydra-core==1.3.2
iopath==0.1.10
Pillow>=10.0.0
sam2 @ git+https://github.com/facebookresearch/sam2.git@main
